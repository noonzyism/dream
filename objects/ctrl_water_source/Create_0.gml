/*
NOTE:	these variables are configurable per-instance in the room editor for this object
		look at this object's variable definitions to modify them
		
dir				default = 0;	// the direction of the emitted waterflow
velocity		default = 1;	// the speed of the emitted waterflow
rate			default = 250;	// a positive value indicating the rate of waterflow emission (lower value = faster)
*/

depth = 5;

alarm[0]		= 1;